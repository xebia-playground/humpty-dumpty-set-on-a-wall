
instruction file load করে
target app URL open করে
browser-এ page inspect করে
app snapshot capture করে
instruction + snapshot combine করে AI কে prompt দেয়
AI Playwright test generate করে
generated test validate করে
browser-এ test run করে
result report return করে